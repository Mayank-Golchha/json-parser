# json-parser
📦 JSON Parser (C++) JSON Parser is a lightweight and fast JSON parser written in modern C++, designed for simplicity, readability, and ease of integration. It can parse standard JSON strings into a usable structure without any external dependencies.
